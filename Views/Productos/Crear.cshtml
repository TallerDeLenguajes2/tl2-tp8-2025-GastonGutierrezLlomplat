@model espacioProductos.Productos

@{
    ViewData["Title"] = "Crear Producto";
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h1 class="h4 fw-normal text-muted mb-4">Crear producto</h1>

            <form asp-action="Crear" method="post">
                @Html.AntiForgeryToken()

                <div class="mb-3">
                    <label asp-for="descripcion" class="form-label text-muted small">Descripción</label>
                    <input asp-for="descripcion" class="form-control" />
                    <span asp-validation-for="descripcion" class="text-danger small"></span>
                </div>

                <div class="mb-4">
                    <label asp-for="precio" class="form-label text-muted small">Precio</label>
                    <input asp-for="precio" type="number" step="0.01" min="0" class="form-control" />
                    <span asp-validation-for="precio" class="text-danger small"></span>
                </div>

                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-dark px-4">Crear</button>
                    <a asp-action="Index" class="btn btn-outline-secondary px-4">Cancelar</a>
                </div>
            </form>
        </div>
    </div>
</div>